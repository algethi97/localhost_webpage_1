# 프로젝트 명: NaJakHome (mini-project_0915 / localhost_webpage_1)

## 1. 프로젝트 개요 및 기술 스택
- **개요**: FastAPI와 ngrok을 기반으로 구축된 반응형 웹 애플리케이션 (대문, 실시간 방명록, 사진 갤러리, 만다린 퍼즐 게임)
- **백엔드**: Python 3.12, FastAPI, Uvicorn, SQLite3
- **프론트엔드**: HTML5, Vanilla JavaScript, CSS3 (모던 미니멀리즘 테마)
- **클라우드 / 외부 서비스**: Supabase (만다린 게임 랭킹 DB), ngrok (외부 터널링)
- **패키지 매니저 & 가상환경**: `uv`
- **핵심 모듈 & 엔진**: Pillow (이미지 검증 및 EXIF 제거, 리사이징), Web Audio API (신디사이저 기반 사운드 엔진)
- **원격 저장소**: [GitHub (algethi97/localhost_webpage_1)](https://github.com/algethi97/localhost_webpage_1) (현재 릴리즈: `v1.1.1`)

## 2. 현재 구현 완료된 기능
- **메인 홈페이지 (`/`)**:
  - 감성적인 웰컴 히어로 섹션 및 3분할(방명록, 갤러리, 게임) 반응형 카드 그리드
  - 페이지 하단 스크롤 감지 시 발동하는 '다만!' 비비드 레드(Vivid Red) 테마 인터랙션
- **실시간 방명록 (`/guestbook`)**:
  - 새로고침 없는 실시간 비동기(Fetch API) 피드 등록 및 렌더링
  - `Enter` 키 즉시 전송 및 `Shift+Enter` 줄바꿈 지원 (한글 조합 중복 전송 방어)
  - 닉네임 최대 20자, 본문 최대 500자 상한 및 실시간 글자 수 카운터 UI (`0/500자`)
  - 자동화 스팸 봇 차단용 허니팟(Honeypot) 필드 적용 (`HTTP 400 Bad Request`)
  - SQLite DB(`data/guestbook.db`) 영구 보관 (파라미터화 쿼리로 SQL Injection 차단)
- **사진 갤러리 (`/photo`)**:
  - 스마트폰 촬영 사진 속 **EXIF GPS 위치 메타데이터 100% 영구 삭제** (개인 거주지/위치 유출 완벽 차단)
  - 스마트폰 회전각(Orientation) 자동 보정 (`ImageOps.exif_transpose`)
  - **최대 30MB 고화소 사진 업로드 허용** (30MB 초과 시 `HTTP 413` 차단)
  - **긴 축 기준 2048px 웹 최적화 리사이징(LANCZOS)**으로 30MB 원본을 1~2MB로 고품질 압축 저장하여 갤러리 로딩 속도 극대화
  - Pillow 기반 위장/손상 파일 3단계 무결성 검증 (`img.verify()`)
  - 고유 UUID 파일명 매핑으로 Path Traversal 원천 차단
  - 반응형 사진 갤러리 그리드 및 클릭 시 원본 라이트박스(Lightbox) 모달 확대
- **만다린 퍼즐 게임 (`/mandarin`)**:
  - 사과게임(Fruit Box) 모티브의 17×10 그리드 감귤 숫자 합 10 만들기 퍼즐
  - Web Audio API 기반 청량한 팝핑(Pop) 사운드 엔진 (상단 HUD 🔊 사운드 온/오프 토글)
  - 120초 카운트다운 타이머, 게이지 바, 남은 귤 카운트, 올클리어 보너스(+50점) 점수 체계
  - Supabase 클라우드 실시간 사이드 Top 5 명예의 전당 랭킹보드 연동
  - 기존 접속자를 위한 `/game` 보조 호환 라우트 유지
- **원클릭 서버 실행기 (`run_server.bat`)**:
  - UTF-8 with BOM 적용으로 Windows 명령 프롬프트(cmd)에서 한글 깨짐 없는 선명한 메뉴 출력
  - `[1] 로컬 디버그 모드` (`http://127.0.0.1:8000`) 및 `[2] ngrok 외부 공개 모드` 대화형 원클릭 선택 지원
- **문서화 & 보안 인프라**:
  - `.gitignore`에 임시 스크래치(`scratch/`), 로그(`*.log`), 테스트 캐시 무시 패턴 추가
  - 최신 기능 명세 및 보안 정책을 반영한 `README.md` 업데이트 완료 (`v1.1.1`)

## 3. 현재 진행 중인 작업 & 문제점
- **현재 상태**: 만다린 게임 이식, 3대 핵심 보안 강화, 원클릭 서버 실행기, README 최신화 및 GitHub 원격 저장소(`main` 브랜치) 푸시(`v1.1.1`)까지 모두 정상 완료됨
- **현재 이슈 / 블로커**: 보고된 치명적 런타임 오류 및 기능 결함 없음 (안정 상태)
- **향후 고려 가능한 개선 과제**:
  - 갤러리에 등록된 부적절한 사진을 브라우저 상에서 삭제할 수 있는 관리자(Admin) 인증/삭제 기능 검토
  - 트래픽 급증 시 SQLite 동시성 락 방어를 위한 WAL(Write-Ahead Logging) 모드 적용 또는 비동기 DB 처리 검토

## 4. 핵심 코딩 규칙
- **패키지 관리**: 항상 `uv add <package>` 및 `uv run <command>` 사용 (`pip install` 직접 사용 절대 금지, 신규 패키지 설치 전 사전 승인 필수)
- **계획 및 실행 분리 원칙**: 계획 요청 시 구현 계획서 작성까지만 수행하며, 사용자의 명시적인 실행 요청("진행해줘" 등) 전에는 임의로 코드를 수정하지 않음
- **사전 질문 및 불확실성 해소 원칙**: 요구사항에 모호함이 있을 경우 임의 판단을 금지하고 반드시 사용자에게 사전 확인 후 작업 착수
- **Git 커밋 및 버전 관리**:
  - 일괄 커밋(`git add .`)을 금지하고 성격별(설정, 기능, 보안, 문서, 개선)로 논리적인 단위 분리 커밋 수행
  - 커밋 메시지는 한국어 접두사(`설정:`, `기능:`, `보안:`, `문서:`, `개선:`)와 버전명(`[v1.1.0]`, `[v1.1.1]`)을 명시
  - 사용자의 명시적 요청 전에는 원격 저장소 푸시 금지
- **Windows 터미널 및 한글 인코딩**:
  - 터미널 출력 및 Git 커밋 시 UTF-8 보장 (`chcp 65001`, `[Console]::OutputEncoding = UTF8`)
  - 배치 파일(`.bat`) 작성 시 cmd.exe 한글 깨짐을 방지하기 위해 UTF-8 with BOM 인코딩을 적용하고 4바이트 유니코드 이모지 배제
- **보안 및 개인정보 보호 원칙**:
  - 사진 업로드 시 EXIF GPS 위치 정보 영구 제거 및 파일 크기/해상도 상한 검증 필수
  - 폼 입력 시 도배 방지를 위한 글자 수 상한 제한 및 허니팟 봇 필터링 적용
  - SQL Injection 방지를 위한 파라미터 바인딩(`?`) 준수 및 XSS 방어(HTML Escape / textContent) 필수

